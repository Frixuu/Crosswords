rootProject.name = "Crossword Helper"
include(":app")

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            version("hilt", "2.41")
            version("kotlin", "1.6.10")
        }
    }
}
